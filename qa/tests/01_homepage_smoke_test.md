---
mode: testing
max_steps: 25
variables:
  BASE_URL:
    value: "https://lt-shoe-store.replit.app"
---

# Homepage loads with hero, navigation and product grid

## Open the store
Open {{BASE_URL}}.
Verify the page loads without errors and the SoleStyle header with navigation links is visible.

## Verify hero section
Verify a hero section is visible near the top of the page.

## Verify product grid
Scroll down if needed and verify a grid of product cards is visible, each showing a product image, name and price.

## Verify cart icon
Verify a cart icon is present in the header.
